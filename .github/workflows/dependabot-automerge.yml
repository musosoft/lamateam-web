name: Dependabot Auto-Merge

on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

permissions:
  contents: write
  pull-requests: write

jobs:
  enable-automerge:
    if: github.actor == 'dependabot[bot]' && github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - name: Fetch Dependabot metadata
        id: metadata
        uses: dependabot/fetch-metadata@v3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Enable auto-merge
        run: |
          PACKAGE_ECOSYSTEM="${{ steps.metadata.outputs.package-ecosystem }}"
          case "${PACKAGE_ECOSYSTEM}" in
            npm|npm_and_yarn|github_actions|github-actions)
              ;;
            *)
              echo "Skipping auto-merge for unsupported ecosystem: ${PACKAGE_ECOSYSTEM}"
              exit 0
              ;;
          esac

          PR_URL="${{ github.event.pull_request.html_url }}"
          if gh pr merge --auto --squash "$PR_URL"; then
            echo "Auto-merge enabled for $PR_URL"
            exit 0
          fi

          echo "Auto-merge is unavailable for this repository/branch."
          echo "Enable 'Allow auto-merge' and add branch protection rules if you want queued auto-merge."
          echo "Skipping failure so this workflow does not error."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
